# 目标（不变量）: 实现 goal v2 全部内容（设计文档 §G 路线 v2 四项），活账本自管
# Updated: turn 1    active: built-smoke-green-pending-reload

> 实战接管账本：用 v1 活账本管「实现 v2」这个真实多步任务（B 场景 + C 内容）。
> v2 权威范围 = `2026-06-20-goal-complete-design.md` §G「v2」四项 + §70 GC 细化。
> 「多目标并存」属 §G「v3（观望）」，**不在本次**。
> 规则：criterion 带 `(id)`；`[x]` 由我用 edit 写=声明，只有 `goal_check(id, cmd:…)` 记下匹配且未漂移证据才渲染 verified，否则 `[!]`；criterion 文本/输入漂移→stale。
> 注意：本会话 live 跑的是 v1 代码；v2 源码改动须 /reload 才生效，故本轮验证=新 smoke 真执行，live dogfood 留待 /reload。

## 验收标准（[ ]未做 / [~]进行中 / [x]已写声明，verified 由证据账判）
- [x] (judge-ev) auto-continue 判官（judge.ts buildGoalJudgePrompt）纳入 cross-check 账本摘要：把 criteria 分成 system-verified / 仅声明[!] / stale 三组作为 DATA 注入，achieved 判定只认 verified 不认裸 `[x]`；GoalJudgeInput 加可选 evidenceLedger，index.ts agent_end 对 doc goal 构造并传入 — 证据: cmd:`npm --prefix agent/skills/pi-astack run smoke:goal-judge-ev`
- [x] (status-viz) `goal_status` 展示证据账：replay 本 goal 的 pi-goal-evidence，渲染最近 N 条 check（criterion/status/kind/相对时间）+ `claimed|verified|stale` 摘要；read-only，doc goal 才显示 — 证据: cmd:`npm --prefix agent/skills/pi-astack run smoke:goal-status-viz`
- [x] (gc-archive) 证据按 active goal_id 隔离（replayGoalEvidenceEvents 加可选 goalId 过滤，旧/已归档 goal 的证据不漏进新 goal；buildLedgerHotzone/goal_status/judge/dedup 全传 state.goal_id）+ gcEvidence 压缩（每 criterion 留最近 verified + 末 K 失败，报归档数）+ staleByTime（N 天无活动→提示，plan.md 不主动删） — 证据: cmd:`npm --prefix agent/skills/pi-astack run smoke:goal-gc`
- [x] (dedup) goal_check 去重缓存：命中相同 input_fp（criterion_text_sha + evidence_sha + 输入文件 sha 全未变）的 verified 未漂移记录时，直接复用、跳过重跑 cmd，返回 `verified (cached)` — 证据: cmd:`npm --prefix agent/skills/pi-astack run smoke:goal-dedup`
- [x] (regression) 全部 goal smoke（含 4 个新 v2 smoke）全绿 + LSP 零诊断 + 旧行为零回归 — 证据: cmd:`bash -c 'for s in goal-state goal-autocontinue goal-doc-tools goal-evidence goal-exec goal-ledger goal-e2e goal-judge-ev goal-status-viz goal-gc goal-dedup; do node agent/skills/pi-astack/scripts/smoke-$s.mjs >/dev/null || exit 1; done; echo ALL-GREEN'`

## 当前状态（整块替换，禁止局部改）
- 方案: evidence.ts 新增 6 helper（summarizeLedgerForJudge / renderEvidenceLog / replay goalId 过滤+flat / gcEvidence / staleByTime / findCachedVerified）+ judge.ts prompt 纳入账本 + index.ts 接 4 处（goal_status viz / agent_end 传账本 / runGoalCheck cmd 去重 / 各处传 goal_id）；dedup 安全边界=仅 cmd 且有声明 inputs 才命中缓存
- 下一步: /reload 后 live dogfood：真触发判官读账本、goal_status 看证据账、dedup 缓存命中、跨 goal_id 隔离
- 阻塞/风险: 无。5 项均 live goal_check 真跑 smoke verified（75 checks 零回归）；index/judge 接线 LSP 零诊断，live 集成验证待 reload（同 v1 路径）

## 决策日志（append-only）
- T0 范围锁定：读设计 §G 确认 v2=判官接账/状态可视化/GC 归档/sha 去重缓存四项；「多目标并存」是 v3，排除（避免把 v3 误当 v2 全量做）
- T1 落地：4 项 v2 + regression 全部 live goal_check verified；新增 4 个 smoke（judge-ev/status-viz/gc/dedup 共 18 checks），全量 11 goal smoke 75 checks 零回归；dedup 选择「仅 cmd + 有声明 inputs」的保守安全边界（避免无声明依赖的测试命令被错误缓存）；GC 的 gcEvidence 在 status-viz 中作为「展示 kept 集 + 报归档数」的承重逻辑，非死代码
