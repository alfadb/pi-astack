node_modules/
.DS_Store
*.log

# pi-astack 所有扩展的产出目录：每个扩展一个子目录
# （.pi-astack/imagine/ 生图、.pi-astack/dispatch/ 临时文件 ...）
.pi-astack/

# sediment 的派生索引与运行时状态（gitignored，可从 markdown 源真重建）
.pensieve/.index/
.pensieve/.state/

# sediment 的 pending queue（如有人在仓内本地起 sediment）
.sediment-pending.jsonl
/skills/browser-tools/package-lock.json

# classifier smoke test artifacts (contains raw LLM output)
smoke-classifier-results.json

# ADR 0036 金标工作目录: 每-query 候选材料(MODE=material 可重建, 嵌入大量 corpus
# 正文不入代码仓)+ 跨厂商标注原始投票文件。最终金标 + 投票计数已在
# scripts/goldset.json(committed), 故工作目录 gitignore。
scripts/.goldset/

# ADR 0036 P4 多向量 eval 的 scratch 输入/输出(LLM 生成 query / 长 entry 尾部 dump,
# 可经 oracle-multivector-eval.mjs DUMP_TAILS + dispatch 重建; 方法论在 ADR §10)。
scripts/.mv-tails.json
scripts/.mv-q-*.json
